# 주간 업무 보고서 생성기 — 개발 기록 (DEVELOPMENT LOG)

> 이 문서는 프로그램이 어떤 요구로 어떻게 만들어졌는지, 모든 결정과 기능을 상세히 기록한 개발 일지입니다.
> 다른 PC에서 이어서 작업하거나, 나중에 동작 이유를 확인할 때 참고하세요.

- **저장소**: https://github.com/joyful6111-cmyk/weekly-report
- **핵심 파일**: `주간보고서_생성.py` (소스), `주간보고서.exe` (PyInstaller 빌드)
- **작성 환경**: Windows, Python 3.14, `openpyxl` / `windnd`, PyInstaller. `gh` CLI 없음.

---

## 1. 프로젝트 개요 / 목표

매주 월요일, 경영지원부 3명의 주간보고 엑셀을 받아 **하나의 부서 통합 주간 보고서(엑셀)** 를 자동 생성한다.

- **입력**: 3명의 주간보고 엑셀 파일 (각 1개)
- **출력**: 기존 수기 PDF 보고서와 **동일한 양식**의 통합 엑셀 (`.xlsx`)
- **핵심 원칙**
  1. 입력 업무 내용 **누락 0** (전부 반영)
  2. 위치·양식은 **기존 PDF 100% 반영**
  3. 결과는 PDF가 아니라 **엑셀**로 (사용자가 엑셀에서 직접 손볼 수 있게)

---

## 2. 입력 형식 (2종, 자동 판별)

NOWSYSTEM 사내 앱에서 내보낸 파일. 버전에 따라 2가지 형식이 있어 `parse_report()`가 자동 판별한다.

| 형식 | 판별 | 파서 | 구조 |
|------|------|------|------|
| HTML형 `.xls` | 앞 2바이트 ≠ `PK` | `_parse_html` (regex) | `<table>` 행, 4열(업무분류/금주진행/진행률/다음주예정), `<br>` 줄바꿈, `[루틴]/[프로젝트]/[신규]` 마커, 하단 게시판 섹션 |
| 진짜 `.xlsx` | 앞 2바이트 = `PK`(zip) | `_parse_xlsx` (openpyxl) | 1행 제목, 3행 헤더, 이후 **A열(업무분류) 병합블록 단위**, 내용은 B열 줄별, 진행률 C열(숫자 0.2=20% 또는 문자 "신규"), 예정 D열, 하단 게시판 |

- ⚠️ Excel "웹페이지로 저장" 형식(.files 폴더 분리형)은 데이터가 외부에 있어 읽기 불가 → 안내창.
- 공통 컬럼 매핑: 입력 `금주 진행`→출력 `지난주 진행`, 입력 `다음주 예정`→출력 `금주 예정`.
- 보고일(월요일) 자동 추정: 헤더 '금주 진행 (시작~종료)'의 **종료일 다음 월요일** (`derive_report_date`).

---

## 3. 출력 양식 (PDF 재현)

- 머리글: `◎ 경영지원부 주간 보고 ◎` + `업무 보고일` + `업무 보고자 : 이동숙 부장`
- **업무 체크리스트** 표 (매주 고정 템플릿, 4행): 업무내용 / 주기 / 담당자
- 본문 표: `구분 | 지난주 진행 업무 | 진행률 | 금주 예정 업무`
  - 첫 행 `데일리 업무` = 고정 템플릿
  - 구분: 경영관리 / 재무업무 / 조달업무·입찰업무 / 지원사업 / R&D사업 / 현장 / 기타업무 / 요청사항
- **📂 부서 공용 게시판 현황** 섹션
- 폰트 맑은 고딕, 연회색 헤더, 테두리, A4 세로·가로 1페이지 맞춤 인쇄설정

---

## 4. 개발 연혁 (요청 → 처리, 시간순)

작업은 사용자의 요청에 따라 점진적으로 이뤄졌다. 각 단계의 **요구와 결정 이유**를 기록한다.

### (1) 최초 제작
- 요구: 3개 엑셀 → PDF 양식 통합 엑셀. 위치 PDF 100% 반영, 내용 누락 0.
- 결정(사용자 확인):
  - 본문 내용 = **원문 100% 전부 반영** (AI 요약·병합 안 함)
  - 3명 업무는 **담당자 표시 없이 구분별 병합**
  - 실행 = **창에서 파일 3개 선택 → 자동 생성**
- 구현: tkinter GUI + regex HTML 파서 + openpyxl 스타일링. 상단 체크리스트·데일리 업무는 고정 템플릿.

### (2) 실행이 안 됨 → 보이는 메인 창 + .exe
- 증상: 파일 선택창이 다른 창 뒤에 숨음(tkinter 대화상자 한계).
- 처리: 파일 선택창만 띄우는 방식 → **눈에 보이는 메인 창**(버튼·목록·보고일·생성버튼)으로 변경.
- 추가: **PyInstaller로 .exe 빌드** → 파이썬 설치 없이 더블클릭 실행.

### (3) 진짜 엑셀(.xlsx) 못 읽음
- 증상: 실제 파일이 HTML형이 아니라 진짜 `.xlsx`(셀 병합 구조)였음 → "내용 없음".
- 처리: `.xlsx` 전용 파서 `_parse_xlsx` 추가, `parse_report`가 형식 자동 판별.

### (4) 수기 보고서와 비교
- 생성본이 수기와 양식 동일·내용 전부 포함 확인.
- 발견된 차이: `[프로젝트]` 행은 입력에서 D(예정)=B(진행) **전체 복제**됨 → 지저분.
- 결정: 예정이 진행과 동일하면 **예정칸 비움** (`_dedup_plan`, 내용 손실 없음).
- 수기의 색강조·요약·묶기는 사람 편집이라 **자동화 안 함** (원문 100% 유지 방침).

### (5) 정렬: [루틴] 위 / [신규] 아래
- 요구: 각 구분 안에서 [루틴]은 맨 위, [신규]는 아래로 몰기.
- 구현: `_task_rank` (루틴=0, 일반=1, 신규=2)로 stable sort.

### (6) 드래그&드롭
- `windnd`(Windows, `force_unicode=True`)로 창·목록칸 hook. 폴더 드롭 시 내부 엑셀 추가.
- 빌드에 `--hidden-import windnd`.

### (7) 중복 업무 제거
- 증상: 같은 업무가 여러 사람 파일에 동일하게 들어 2번 표시.
- 처리: 같은 구분 내 **완전 동일 업무는 1개만**(`seen_keys`).

### (8) 비슷한 업무 합치기 확인 팝업
- 요구: 다른 구분(예: 경영관리 vs R&D)에 비슷한 업무가 있으면 합칠지 팝업.
- 구현: `find_similar_pairs`(difflib, 동일업무는 묶어 비교) + `resolve_similar_pairs` 팝업.
- 기준: `SIMILARITY_THRESHOLD = 0.55` (30%는 무관한 신규까지 잡혀 상향).

### (9) AI 요약본(선택)
- 요구: 기본 보고서 외 **AI 요약본** 별도 파일 생성 (구분별 핵심 + 완료/진행중/예정 둘 다).
- 구현: Claude API(`urllib` 직접 호출, SDK 미사용) → JSON 응답 → 2섹션 xlsx.
- API 키: exe 옆 `claude_api_key.txt` 또는 env `ANTHROPIC_API_KEY`, GUI '키 설정'에서 입력·저장.
- 모델 여러개 순차 시도(`AI_MODELS`). 실패해도 기본 보고서는 생성.
- ⚠️ **HTTP 400 "credit balance too low"** = 코드 문제 아님, **API 계정 결제 크레딧 부족**. console.anthropic.com → Plans & Billing 충전 필요. (ChatGPT/Claude 구독과 별개의 종량제 결제)

### (10) [루틴]/[프로젝트] 글자 삭제
- 요구: 수기처럼 본문에서 `[루틴]`,`[프로젝트]` 표시 제거.
- 구현: `_strip_markers` (표시용만 제거, 정렬·중복판단엔 원문 사용). `[신규]`는 유지.

### (11) 세금계산서/분납서 재무 맨 위 고정
- 요구: 게시판의 세금계산서·분납서를 재무업무 맨 위 고정, 내용은 매주 입력 반영.
- 구현: `재무_고정_게시판`. (이후 13번에서 중복·이름 문제로 보강)

### (12) 조달/입찰 표준 업무 맨 위 고정
- 요구: 조달업무/입찰업무에 매주 들어오는 표준 4건을 맨 위 고정.
- 구현: `조달입찰_고정_키워드`(검색리스트/입찰업무정리/조달진행상황/조달관련그룹웨어공지)로 입력에서 찾아 키워드 순 고정. (현 샘플엔 미포함 → 실제 박선하 파일 필요)

### (13) 이름 제거 + 세금계산서/분납서 중복 정리
- 요구: 게시판에 작성자 이름([이동숙] 등)이 보고서에 나오면 안 됨. 세금계산서/분납서 중복 제거.
- 구현:
  - `_strip_names` (`이름목록` = 이동숙/박선하/김유미 태그 제거, '업무 보고자' 명의는 유지)
  - 세금계산서/분납서는 라벨별 1줄로 합치고 `ㅇㅇㅇ` 같은 placeholder(`_is_placeholder`) 제외.

### (14) 부서 공용 게시판 통합 + 충돌 확인 팝업
- 요구: 게시판은 항목별 1개만. 파일마다 내용 다르면 어느 것 반영할지 팝업.
- 구현: `resolve_board`(라벨별 통합) + `_board_conflict_dialog`(파일별 선택/모두 합치기).
  `build_workbook`이 `all_boards` 대신 통합된 `merged_board=[(label,content,status)]`를 받도록 변경.

### (15) 데일리 업무 중복 확인 팝업
- 요구: 상단 '데일리 업무'에 이미 있는 항목이 본문 아래에 또 나오면 안 됨. 확인 팝업.
- 구현: `find_daily_duplicates`(`데일리_항목`과 겹침 검출, 동일내용 1개로 묶음) + `resolve_daily_duplicates`(빼기/두기, 기본 빼기).

### (16) 게시판 내용 날짜 제거 (정정 포함)
- 1차: 모든 날짜 제거 → 사용자 정정: **`날짜_` 접두만** 제거(`-6/4_새동초`→`-새동초`), 그 외 날짜(2026-05-29, 6월4일, 3/6일)는 **유지**.
- 구현: `_strip_dates` (resolve_board에서 게시판 내용에만 적용). IP·금액 보존.

### (17) 셀 위아래 여백
- 요구: 각 칸 위아래 여백. 이후 "아래 3칸"으로(엑셀→PDF 시 아래 잘림 방지).
- 구현: `_pad`로 셀 값을 `\n*PAD_TOP + 내용 + \n*PAD_BOTTOM`. 현재 `PAD_TOP=1`, `PAD_BOTTOM=3`.

### (18) 같은 구분 내 유사 업무 군집
- 요구: 같은 구분 안에서 유사 내용끼리 인접 정렬 (예: 신규입사자 업무들 연달아).
- 구현: `_arrange`(rank 그룹 후 각 그룹 `_cluster`). `_cluster`=전이적 그리디,
  `_topic`(머리표·괄호·% ·filler단어[관련/업무/처리 등] 제거한 핵심어)의 유사도 `>= CLUSTER_THRESHOLD(0.42)`.
  - 예: 신입사원/신규입사자/신규입사직원(철자 다른 동의어) 4건 인접, "신용보증기금"은 분리.

### (19) GitHub 업로드
- 주간보고 관련 파일만 모은 새 폴더 `weekly-report-generator`로 git 저장소 구성(API 키 제외).
- `joyful6111-cmyk/weekly-report`에 push (Git Credential Manager 브라우저 인증).

---

## 5. 처리 파이프라인 (generate 순서)

1. 파일 파싱 (`parse_report`) — 읽기 실패 파일은 건너뜀
2. (체크 시) **데일리 중복 확인** 팝업 → 본문에서 뺄지
3. (체크 시) **비슷한 업무 확인** 팝업 → 합칠지
4. **게시판 통합** (`resolve_board`) → 파일마다 다르면 충돌 팝업
5. **보고서 생성** (`build_workbook`) → 저장 → 열기
6. (체크 시) **AI 요약본** 생성

---

## 6. 주요 설정 (`주간보고서_생성.py` 상단 `[설정]`)

| 설정 | 의미 |
|------|------|
| `부서명`, `보고자` | 머리글 |
| `체크리스트`, `데일리_지난주/금주`, `데일리_항목` | 고정 상단표 + 데일리 중복 판별 항목 |
| `구분_매핑`, `구분_순서` | 업무분류→구분 |
| `이름목록` | 게시판 작성자 이름 제거 대상 |
| `재무_고정_게시판`(=세금계산서·분납서), `재무_고정_구분` | 재무 맨 위 고정 |
| `조달입찰_고정_구분`, `조달입찰_고정_키워드` | 조달/입찰 맨 위 고정 표준업무 |
| `SIMILARITY_THRESHOLD`(0.55) | 다른 구분 비슷업무 검출 |
| `CLUSTER_THRESHOLD`(0.42), `_CLUSTER_STOP` | 같은 구분 유사 군집 |
| `PAD_TOP`(1), `PAD_BOTTOM`(3) | 셀 위/아래 여백 |
| `AI_MODELS`, `AI_MAX_TOKENS`, `AI_KEY_FILE` | AI 요약 |

---

## 7. 빌드 / 실행 / 배포

```powershell
# 실행(개발)
pip install -r requirements.txt
python 주간보고서_생성.py

# .exe 재빌드 (설정/코드 수정 후 반드시 다시 빌드해야 exe에 반영됨)
python -m PyInstaller --noconfirm --onefile --windowed --name 주간보고서_생성기 `
  --hidden-import openpyxl --hidden-import windnd 주간보고서_생성.py
# dist\주간보고서_생성기.exe 를 주간보고서.exe 로 복사해 사용
```

검증 팁:
- xlsx → PDF 렌더 확인: Excel COM `ExportAsFixedFormat(0, pdf)` 후 PyMuPDF(`fitz`)로 이미지화.
- 한글 경로는 Bash 도구에서 깨질 수 있어 PowerShell + utf-8 stdout 권장.

---

## 8. 다른 PC에서 이어서 작업하기

```powershell
# 1) 클론
git clone https://github.com/joyful6111-cmyk/weekly-report.git
cd weekly-report

# 2) 라이브러리 설치
pip install -r requirements.txt

# 3) (AI 요약 쓸 경우) claude_api_key.txt 새로 만들기 — git에는 없음(.gitignore)
#    프로그램의 'API 키 설정' 버튼으로 입력하면 자동 저장됨

# 4) 작업 후 올리기
git add -A
git commit -m "수정 내용"
git push
```

> ⚠️ `claude_api_key.txt`(비밀 키)와 생성된 보고서(`경영지원부_주간보고_*.xlsx`)는 `.gitignore`로 제외됨. 절대 커밋 금지.

---

## 9. 알려진 한계 / 향후 과제

- 수기 보고서의 **의미 기반 요약·재편집**(색강조, 문장 다듬기)은 규칙으로는 한계 → 필요 시 AI 요약본 활용.
- 유사 업무 군집은 문자열 유사도 기반이라 동의어·맥락을 완벽히는 못 잡음 (`CLUSTER_THRESHOLD`로 조절).
- 조달/입찰 표준 4건 고정은 입력 문구가 키워드와 다르면 미매칭 → `조달입찰_고정_키워드` 수정 필요.
- AI 요약은 Anthropic API **결제 크레딧** 필요.
